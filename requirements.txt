fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
sqlalchemy==2.0.35
alembic==1.13.3
pyyaml==6.0.2
httpx==0.27.2
tenacity==9.0.0
pybit==5.8.0
hyperliquid-python-sdk==0.23.0   # ≥ 0.10 pulls in modern eth-account (0.13+),
                                  # which dropped the parsimonious dep that
                                  # broke on Python 3.11 (inspect.getargspec).
jinja2==3.1.4
python-multipart==0.0.12
tzdata==2024.2                    # zoneinfo fallback DB — slim images ship no
                                  # system tzdata, so dashboard tz conversion
                                  # (America/Toronto) needs this.
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==7.1.0                  # coverage gate in CI (.github/workflows/ci-cd.yml)
hypothesis==6.155.1                # property-based tests for fill-math / PnL invariants
